#include <stdio.h>
#include <stdlib.h>

int down_sum(int n){
    int result = 0;
    while (n--){
        result += n;
    }
    return result;
}

int main()
{
    int iter;
    scanf("%d", &iter);

    while(iter--){
        int x, y;
        scanf("%d %d", &x, &y);

        
        
    }

    return 0;
}