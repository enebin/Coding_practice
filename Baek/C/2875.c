#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, m, k;
    scanf("%d %d %d", &n, &m, &k);

    int count;
    int sum = n + m;

    while (sum - (n + m) < k){
        if (n / 2 > m){

        }
    }

    printf("%d\n", count);

    return 0;
}