#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;
typedef vector<int> vi;
vi nums;

vi merge_sort(vi arr){
    int s = arr.size();
    if (s > 1){
        vi left = 
        merge_sort(arr[])
    }
}

int main(){
    int iter;
    cin >> iter;
   
    
    while(iter--){
        int temp;
        cin >> temp;

        nums.push_back(temp);
    }


}