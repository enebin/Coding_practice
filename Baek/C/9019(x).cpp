#include <iostream>
#include <algorithm>
#include <queue>
#include <vector>

using namespace std;

queue<int> q;
vector<vector<int>> visited(10001, vector<int>(20, 0));
 
void bfs(int n, int m){
    q.push(n);

    while (!q.empty()){
        int cur = q.front;
        q.pop;

        if (cur == m)
            for (vector<int> vec : visited){
                for (int n )
            }
            break;


    }

}

int main(){
    int iter;
    cin >> iter;

    while (iter--){
        int n, m;
        cin >> n >> m;


    
    
    
    }
}