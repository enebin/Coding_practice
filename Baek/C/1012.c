#include <stdio.h>
#include <stdlib.h>

typedef struct lettuce{
    int 

}

int main()
{
    int iter;
    scanf("%d", &iter);

    while(iter--){
        int n, m, k;
        scanf("%d %d %d", &n, &m, &k);

        int x, y;
        for (int i=0; i<k; i++){
            scanf("%d %d", &x, &y);
            loc[x][y] = 1;
        }

    }

    return 0;
}